import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mundom.creatumascota',
  appName: 'Mundo M. Crea tu mascota',
  webDir: 'dist',
};

export default config;
